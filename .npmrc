# Keep optional peer deps (e.g. @xenova/transformers for provider-local) out of
# the default install — they are opt-in. Without this, pnpm auto-installs peers,
# pulling heavy ML packages (onnxruntime, sharp) into every install and CI run.
auto-install-peers=false
